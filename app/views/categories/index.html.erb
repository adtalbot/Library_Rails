<div class='container'>
  <h1>Library</h1>

  <ul>
    <% @categories.each do |category| %>
    <li><%= link_to category.name, category_path(category) %></li>
    <% end %>
  </ul>
  <p><%= link_to 'Add a Category', new_category_path %></p>
</div>
